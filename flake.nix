{
  description = "A pipe-optimized library for Nix, that escapes from general Nix's use-case, and delivers more generic functional programming functions. Heavily inspired by `elm/core`.";
  inputs = { };
  outputs = { self }: import ./default.nix;
}
